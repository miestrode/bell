fn factorial(number: int) {
    let result = 1;

    while number > 1 {
        result = result * number;

        number = number - 1;
    }

    result
}

fn main() {
    let current = 1;
    
    while current < 8 {
        println(factorial(current));
        current = current + 1;
    }
}
